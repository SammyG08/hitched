name: hitched_backend
description: A new Dart Frog server for Hitched wedding app.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.0.0
  sqlite3: ^2.4.0
  drift: ^2.16.0
  json_annotation: ^4.9.0
  intl: ^0.19.0
  google_generative_ai: ^0.4.5

dev_dependencies:
  build_runner: ^2.4.9
  drift_dev: ^2.16.0
  json_serializable: ^6.8.0
  mocktail: ^1.0.0
  test: ^1.19.0
